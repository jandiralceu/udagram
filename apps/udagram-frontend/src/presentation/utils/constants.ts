/**
 * Query keys used for React Query caching and invalidation.
 * These constants ensure consistency across the application when accessing or mutating data.
 */
export class QueryKeys {
  static readonly me = 'me'
  static readonly feeds = 'feeds'
}
