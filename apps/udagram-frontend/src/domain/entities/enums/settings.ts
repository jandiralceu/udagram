/**
 * Application Settings Definitions
 *
 * This module defines the core configuration types and constants used for
 * global application settings, including UI theming and localization.
 *
 * @packageDocumentation
 * @module Domain/Entities/Enums
 */

/**
 * Defines the available visual themes for the application interface.
 * - `light`: Standard light mode with high brightness backgrounds.
 * - `dark`: Dark mode for low-light environments.
 */
export type ThemeMode = 'light' | 'dark'
